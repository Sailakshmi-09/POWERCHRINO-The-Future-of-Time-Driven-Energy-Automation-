#include "types.h"   // Includes user-defined data types such as u32

// Function declaration to generate a delay in microseconds
// Parameter:
//   u32 - Delay duration in microseconds
void delay_us(u32);

// Function declaration to generate a delay in milliseconds
// Parameter:
//   u32 - Delay duration in milliseconds
void delay_ms(u32);

// Function declaration to generate a delay in seconds
// Parameter:
//   u32 - Delay duration in seconds
void delay_s(u32);
